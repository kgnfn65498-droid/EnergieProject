# EnergieProject v32.4.37 Closure Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Close the remaining 32.4.x regressions without adding new functionality.

**Architecture:** Keep the existing release/watcher/ProjectManagerV2 design. Add bounded reconciliation for watcher container configuration, explicit native-MCP runtime fingerprint/reload acceptance, transaction-safe CR retention, and post-release maintenance/live-acceptance gates. Preserve CLEARUP quarantine-only behavior and existing security boundaries.

**Tech Stack:** Python 3, pytest, POSIX shell, QNAP Docker/Container Station, Home Assistant add-on release payload.

**Spec:** User-approved 32.4.37 closure list derived from the 32.4.36 handover and live audit.

## Global Constraints
- No production deployment from this build workspace.
- TDD RED -> GREEN for each defect.
- No generic shell/root/Docker control surface.
- No TLS/certificate route revival.
- CR retention maximum 1 canonical valid set per type, fail-closed.
- CLEARUP remains fresh-audit + quarantine/move only; no delete.
- Exact-artifact fresh-extract verification required before delivery.

---

### Task 1: Watcher upgrade reconciliation
- Add failing tests proving a 4.35-style watcher cannot satisfy the 4.37 container contract after mere exec-refresh.
- Add an explicit watcher container contract/fingerprint and bounded reconciliation trigger through the existing trusted local release path.
- Verify docker.sock mount, network/capability/security options and no generic Docker API exposure.

### Task 2: Native MCP runtime consistency
- Add failing tests for source/runtime fingerprint mismatch after native-MCP hotfix.
- Add explicit runtime-state marker and protected reload request for exactly energie-filesystem-mcp.
- Block CR acceptance while source/runtime fingerprints differ.

### Task 3: Transaction-safe CR retention
- Add failing tests for mid-commit failures in NAS and EnergieProject CR retention.
- Replace direct destructive commit with atomic quarantine/move semantics plus readback validation.
- Verify old valid set remains recoverable on any failure and canonical directory contains exactly one valid set only after full GREEN.

### Task 4: CLEARUP and PM-health closure
- Add tests for fresh plan-id only, hard-move/old-path-absent, symlink/live-reference REVIEW behavior, origin/restore manifest, and no deletes.
- Add tests that PM-health selects only canonical CR sets and never stale paths/evidence.

### Task 5: Mode/post-release acceptance state
- Add tests for release -> post-release-maintenance-required -> maintenance acceptance -> user end-state.
- Keep failure fail-closed in maintenance; no silent USER transition.

### Task 6: Legacy TLS reachability and documentation
- Prove active UI/POST/orchestrator/service cannot route to TLS setup/activation.
- Record symptom -> root cause -> fix -> regression prevention in changelog/roadmap/project agreements.

### Task 7: Release identity and complete verification
- Set all release identities to 32.4.37 / next PM rc identity if required by established pattern.
- Regenerate manifests via canonical builder.
- Run targeted tests, complete 174-module suite, syntax checks, fresh extract, exact-artifact preflight and final SHA256.
