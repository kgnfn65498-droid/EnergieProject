# v32.3.17 Idempotent GitHub Publication Implementation Plan

**Goal:** Make HA publication compatible with the project-standard MAIN-before-live release path without weakening fail-closed publication safety, and use risk-guided pre-delivery verification.

### Task 1 — Reproduce and classify the stale publication state
- Add tests for previous baseline, exact target, metadata-only target source match and real target source mismatch.
- Confirm current 32.3.16 fails the new target-prepublished tests.

### Task 2 — Implement fail-closed idempotent publication
- Add source fingerprinting excluding only `MANIFEST.sha256` and `SHA256SUMS.json`.
- Classify remote state before synchronization.
- Verify remote files against the manifest for `target_exact`.
- Finish exact target without a push; allow metadata-only target source match through the existing validated sync/push path.
- Leave mismatches blocked.

### Task 3 — Release identity and verification policy
- Set release identity to 32.3.17 and document the change.
- Preserve v32.3.16 as historical changelog evidence.
- Add risk-guided release verification instructions with mandatory pre-delivery publication checks.

### Task 4 — Verification and publication
- Run focused publisher/release tests.
- Run one complete source suite after final changes.
- Review scoped diff; merge feature branch to MAIN.
- Build definitive ZIP from merged MAIN with regenerated manifest/SHA metadata.
- From a clean ZIP extraction: verify ZIP/path safety, manifest/SHA, Python compile, shell syntax, focused publication tests and complete regression suite.
- Simulate the target-prepublished publication state because this release changes publication behavior.
