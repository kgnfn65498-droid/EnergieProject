# v32.3.18 Live Mode GUI Refresh Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make mode/HOLD GUI actions immediately render fresh measured status in Home Assistant Ingress and correct blocked hold feedback.

**Architecture:** Keep server-side POST handlers and PRG redirects intact. Add a small client-side form submitter scoped to `#operating-mode-card form` that follows the existing 303 and navigates to the redirected URL, forcing a fresh server-rendered snapshot; add fixed error classification for blocked hold validation.

**Tech Stack:** Python 3.12, stdlib HTTP server wrapper, HTML/JavaScript, pytest.

## Global Constraints
- Do not change mode profiles, controller semantics, HOLD gates, Crash Recovery behavior, watcher, scheduler, month-close logic or non-GUI JSON API.
- Use fixed notice codes only; no untrusted HTML.
- Preserve Home Assistant Ingress-relative URLs.
- DEVELOPMENT remains active until explicit user confirmation.

---

### Task 1: Fresh GUI navigation

**Files:**
- Modify: `slimmemeterportal_import/rootfs/app/operating_mode_web.py`
- Create: `tests/test_v32318_live_mode_gui_refresh.py`

**Interfaces:**
- Consumes: existing GUI forms with `return_ui=1`, existing 303 redirect contract.
- Produces: JavaScript submit handler scoped to the mode card; fresh navigation after POST.

- [ ] Write tests asserting the card contains a scoped submit listener, same-origin `fetch`, `FormData`, `cache: "no-store"`, redirected URL navigation, fallback reload, and no generic interception outside the mode card.
- [ ] Run the new tests and confirm they fail against v32.3.17.
- [ ] Implement the minimal scoped submit handler in `render_mode_card()`.
- [ ] Run new and v32.3.15 GUI tests and confirm they pass.

### Task 2: Correct blocked HOLD feedback

**Files:**
- Modify: `slimmemeterportal_import/rootfs/app/operating_mode_web.py`
- Modify: `tests/test_v32318_live_mode_gui_refresh.py`

**Interfaces:**
- Consumes: `attempt_release_hold()` result dictionary.
- Produces: fixed notice code `release_hold_blocked` with error level for blocked validation.

- [ ] Add failing unit tests for released, already-released/reconcile-safe, blocked and exception cases.
- [ ] Implement a result-to-notice helper returning `(notice_code, level)` and add fixed banner text for `release_hold_blocked`.
- [ ] Keep exceptions mapped to `action_failed/error` and non-GUI JSON unchanged.
- [ ] Run all GUI/HOLD tests.

### Task 3: Release identity and regression

**Files:**
- Modify: `VERSIE.txt`
- Modify: `slimmemeterportal_import/config.yaml`
- Modify: `slimmemeterportal_import/rootfs/app/main.py`
- Modify: `slimmemeterportal_import/rootfs/app/mode_entrypoint.py`
- Modify: `CHANGELOG.md`
- Modify: `slimmemeterportal_import/CHANGELOG.md`
- Create: `tests/test_v32318_release_identity.py`
- Update only current-version assertions in legacy tests where required.

- [ ] Add failing v32.3.18 release-identity test.
- [ ] Bump current release identity to 32.3.18 and update current-version assertions only.
- [ ] Run focused GUI/HOLD/release tests.
- [ ] Run the full suite once from source.

### Task 4: Definitive MAIN package and Crash Recovery review

**Files:**
- No production changes unless the review finds a concrete conflict; any such conflict requires a new scoped hotfix rather than silent expansion of v32.3.18.

- [ ] Sync the exact tested branch source to GitHub and verify the changed-file scope.
- [ ] Merge to MAIN and verify MAIN tree equals the tested feature tree.
- [ ] Build the definitive ZIP from MAIN; regenerate manifest/checksum metadata.
- [ ] Run the full suite directly from the extracted definitive ZIP.
- [ ] Run focused mode/HOLD GUI tests and installer upgrade simulation from 32.3.17 to 32.3.18.
- [ ] Review `recovery_core.py`, `recovery_extensions.py`, Crash Recovery web/runtime entrypoints, `operating_mode_crash_recovery.py`, `mode_entrypoint.py`, installer/watcher gates and cleanup/restore paths for mode conflicts.
- [ ] Report any blocker before live Crash Recovery use; otherwise mark the static Crash Recovery/mode compatibility review PASS.
