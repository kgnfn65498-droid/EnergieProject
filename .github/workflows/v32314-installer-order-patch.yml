name: v32.3.14 installer publication ordering patch cleanup

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "temporary patch workflow disabled; pending deletion"
